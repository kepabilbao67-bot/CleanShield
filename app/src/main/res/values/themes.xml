<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CleanShield" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_light</item>
        <item name="colorOnPrimaryContainer">@color/primary_dark</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_light</item>
        <item name="colorOnSecondaryContainer">@color/secondary_dark</item>

        <!-- Tertiary -->
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorOnTertiary">@color/on_tertiary</item>

        <!-- Error -->
        <item name="colorError">@color/blocked_red</item>
        <item name="colorOnError">@color/on_blocked</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/surface_light</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/primary_dark</item>
    </style>

    <style name="Theme.CleanShield.Blocked" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blocked_red</item>
        <item name="colorOnPrimary">@color/on_blocked</item>
        <item name="android:colorBackground">@color/blocked_red_dark</item>
        <item name="android:statusBarColor">@color/blocked_red_dark</item>
        <item name="android:navigationBarColor">@color/blocked_red_dark</item>
    </style>
</resources>
